@model ContatoModel
@{
    ViewData["Title"] = "Contatos - Deletar";
}

<div class="container text-center">
    <div class="row align-items-center">
        <div class="col">
            <h3>Deseja <strong>REALMENTE</strong> deletar o contato <strong>@Model.Nome</strong>?</h3>
        </div>
        <div class="row align-items-center">
            <div class="col">
                <a role="button" class="btn btn-danger" asp-route-id="@Model.Id" asp-controller="Contato" asp-action="Delete">Sim, desejo deletar o contato</a>
            </div>
            <div class="col">
                <a role="button" class="btn btn-secondary" asp-controller="Contato" asp-action="Index">Não, voltar para lista de contatos</a>
            </div>
        </div>
    </div>
</div>